window.addEventListener('load', function() {
    var circleContainer = document.getElementById('circleContainer');
    var circleImage = document.getElementById('circleImage');

    circleContainer.style.width = circleImage.width + 'px';
    circleContainer.style.height = circleImage.height + 'px';
});
